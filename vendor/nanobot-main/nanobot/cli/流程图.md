# 交互模式泳道图（`nanobot agent` 不带 `-m`）

下面这张是我在聊天里输出的“那一版”（尽量保持原样，方便你对照你当时看的内容）。

```text
时间向下
┌────────────────────────────── Input（用户输入） ──────────────────────────────┐
│ 1) run_interactive() 里进入 while True 循环                                   │
│    代码：agent() → run_interactive() → while True                             │
│                                                                              │
│ 2) 读一行输入 user_input                                                     │
│    user_input = await _read_interactive_input_async()                        │
│    代码：commands.py: 输入循环里                                               │
│                                                                              │
│ 3) 如果是 exit/quit：恢复终端并 break                                         │
│    if _is_exit_command(command): _restore_terminal(); break                  │
│                                                                              │
│ 4) 开始新一轮：turn_done.clear()（灯灭：本轮未结束）                          │
│    turn_done.clear()                                                        │
│                                                                              │
│ 5) 创建 renderer（用来处理流式 delta）                                        │
│    renderer = StreamRenderer(...)                                            │
│                                                                              │
│ 6) 把输入封装成 InboundMessage 丢进 inbound 队列                              │
│    await bus.publish_inbound(InboundMessage(..., metadata={"_wants_stream":True}))│
│                                                                              │
│ 7) 等待本轮结束（灯亮后才继续）                                               │
│    await turn_done.wait()                                                    │
│                                                                              │
│ 8) 回合结束后：                                                              │
│    - 非流式：turn_response 有值 → _print_agent_response(...)                  │
│    - 流式：renderer 已输出过 → 这里只 close renderer                          │
└──────────────────────────────────────────────────────────────────────────────┘


┌────────────────────────────── Agent（智能体处理） ────────────────────────────┐
│ A) 后台常驻任务启动：                                                        │
│    bus_task = asyncio.create_task(agent_loop.run())                          │
│    代码：run_interactive() 开头                                               │
│                                                                              │
│ B) agent_loop.run() 内部无限循环：                                            │
│    msg = await bus.consume_inbound()                                         │
│    代码：nanobot/agent/loop.py: run()                                         │
│                                                                              │
│ C) 收到 InboundMessage 后：                                                   │
│    - /stop /status 等直接处理                                                 │
│    - 普通消息 → create_task(self._dispatch(msg))                             │
│    代码：nanobot/agent/loop.py: run()                                         │
│                                                                              │
│ D) _dispatch(msg) 里（重要）：                                                │
│    如果 msg.metadata["_wants_stream"]：定义 on_stream/on_stream_end            │
│    这些回调会 publish_outbound：                                              │
│      - delta → metadata={"_stream_delta": True}                              │
│      - end   → metadata={"_stream_end": True, "_resuming": resuming}         │
│    代码：nanobot/agent/loop.py: _dispatch()                                   │
│                                                                              │
│ E) _process_message(...) → _run_agent_loop(...)：                             │
│    - progress: publish_outbound(metadata={"_progress":True, "_tool_hint":...})│
│    - 最终：publish_outbound(OutboundMessage(content=final, ...))              │
│    - 若走流式：最后返回的 OutboundMessage 会带 meta["_streamed"]=True          │
│    代码：nanobot/agent/loop.py: _process_message/_run_agent_loop              │
└──────────────────────────────────────────────────────────────────────────────┘


┌────────────────────────────── Output（终端渲染） ─────────────────────────────┐
│ a) 后台常驻任务启动：                                                        │
│    outbound_task = asyncio.create_task(_consume_outbound())                   │
│    代码：run_interactive()                                                    │
│                                                                              │
│ b) _consume_outbound() 内部无限循环：                                         │
│    msg = await bus.consume_outbound()                                        │
│    代码：commands.py: _consume_outbound()                                     │
│                                                                              │
│ c) 分 4 类消息处理（完全看 msg.metadata）：                                   │
│    1) _stream_delta  → await renderer.on_delta(msg.content)                  │
│    2) _stream_end    → await renderer.on_end(resuming=msg.metadata["_resuming"])│
│    3) _progress      → 打印进度（可能按配置丢弃）                              │
│    4) 最终结束信号：                                                          │
│       - _streamed → turn_done.set()（灯亮：本轮结束）                         │
│       - 非流式最终回复 → turn_response.append(...) + turn_done.set()          │
└──────────────────────────────────────────────────────────────────────────────┘
```

## 你只要记住 3 个“连接点”（就不乱了）

1) Input → Agent：`bus.publish_inbound(...)` → `agent_loop.run()` 的 `consume_inbound()`
2) Agent → Output：`bus.publish_outbound(...)` → `_consume_outbound()` 的 `consume_outbound()`
3) Output → Input：`turn_done.set()` 让输入侧的 `await turn_done.wait()` 放行（表示这一轮结束）

## 对照代码你可以按这个顺序定位

- 输入循环：`nanobot/cli/commands.py` 里 `run_interactive()` 的 `while True`（按关键字搜 `turn_done.clear()`）
- 输出消费者：同文件里 `_consume_outbound()`（搜 `_stream_delta`）
- Agent 常驻：`nanobot/agent/loop.py:run()`（搜 `consume_inbound`）
- 流式标记生成：`nanobot/agent/loop.py:_dispatch()`（搜 `_stream_delta` / `_stream_end`）

---

# 单次模式流程图（`nanobot agent -m "..."`）

下面这张描述 `nanobot agent -m "Hello!"` 的执行路径（特点：**不走 bus 队列**，直接调用 `AgentLoop.process_direct()`）。

```text
┌──────────────────────────── CLI（commands.py） ────────────────────────────┐
│ argv: ["nanobot", "agent", "-m", "hi"]                                     │
│                                                                            │
│ agent(message="hi", session_id=..., ...)                                   │
│   ├─ config = _load_runtime_config(config, workspace)                      │
│   ├─ sync_workspace_templates(config.workspace_path)                        │
│   ├─ bus = MessageBus()                                                    │
│   ├─ provider = _make_provider(config)                                     │
│   ├─ cron = CronService(get_cron_dir()/jobs.json)                           │
│   ├─ agent_loop = AgentLoop(bus=bus, provider=provider, cron_service=cron, ...)│
│   └─ asyncio.run(run_once())                                               │
│        ├─ renderer = StreamRenderer(...)                                   │
│        ├─ response = await agent_loop.process_direct(                      │
│        │       message, session_id, on_progress=_cli_progress,              │
│        │       on_stream=renderer.on_delta, on_stream_end=renderer.on_end   │
│        │   )                                                               │
│        ├─ 若没流式：close renderer + _print_agent_response(...)             │
│        └─ await agent_loop.close_mcp()                                     │
└────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────── Agent（agent/loop.py） ────────────────────────┐
│ process_direct(content, session_key, channel="cli", chat_id="direct", ...)  │
│   ├─ await _connect_mcp()                                                  │
│   ├─ msg = InboundMessage(channel, sender_id="user", chat_id, content)     │
│   └─ return await _process_message(msg, session_key=..., on_progress=...,   │
│                                   on_stream=..., on_stream_end=...)        │
│                                                                            │
│ _process_message(...)                                                      │
│   ├─ session = sessions.get_or_create(session_key)                         │
│   ├─ history = session.get_history(...)                                    │
│   ├─ initial_messages = context.build_messages(system+history+user)        │
│   ├─ final_content, _, all_msgs = await _run_agent_loop(...)               │
│   ├─ _save_turn(...); sessions.save(...); maybe_consolidate(...)           │
│   └─ return OutboundMessage(content=final_content, ...)                    │
└────────────────────────────────────────────────────────────────────────────┘
```

你可以抓住这条路径的 3 个关键词：
1) **direct**：`process_direct()`（不通过 inbound/outbound 队列）
2) **一次性**：没有输入循环/turn_done，只处理这一条 message
3) **同样支持流式**：通过 `on_stream/on_stream_end` 回调把 delta 打到 CLI

---

# Gateway 模式泳道图（`nanobot gateway`）

下面这张描述 `nanobot gateway` 的整体运行方式：**一个进程里并发跑 4 类长期任务**
- `agent.run()`：消费 inbound，产出 outbound（核心智能体）
- `channels.start_all()`：启动所有启用的渠道，并在后台把平台消息 publish 到 inbound
- `cron.start()`：到点触发定时任务（触发时回调 `on_cron_job`，进而调用 agent）
- `heartbeat.start()`：周期性检查/执行 HEARTBEAT 任务（执行/通知也会走 agent + bus）

```text
时间向下

┌──────────────────────────── CLI 启动（commands.py） ────────────────────────────┐
│ argv: ["nanobot", "gateway", ...]                                             │
│                                                                              │
│ gateway(...)                                                                  │
│  1) config = _load_runtime_config(...)                                        │
│  2) sync_workspace_templates(...)                                             │
│  3) bus = MessageBus(); provider = _make_provider(config)                     │
│  4) session_manager = SessionManager(...)                                     │
│  5) cron = CronService(...); agent = AgentLoop(..., cron_service=cron, ...)   │
│  6) cron.on_job = on_cron_job  （回调：到点后把任务交给 agent.process_direct）  │
│  7) channels = ChannelManager(config, bus)                                    │
│  8) heartbeat = HeartbeatService(... on_execute/on_notify ...)                │
│  9) asyncio.run(run())                                                        │
│     └─ await cron.start(); await heartbeat.start()                             │
│     └─ await asyncio.gather(agent.run(), channels.start_all())                │
│                                                                              │
│ 退出/异常：finally 里 close_mcp/stop cron/stop heartbeat/stop channels         │
└──────────────────────────────────────────────────────────────────────────────┘


┌──────────────────────────── Channel 侧（channels/*） ─────────────────────────┐
│ channels.start_all()                                                         │
│  - 启动 outbound dispatcher：从 bus.consume_outbound() 取消息并发回平台        │
│  - 启动每个启用的 channel.start()：监听平台事件                               │
│                                                                              │
│ 平台来消息 → BaseChannel._handle_message(...)                                 │
│  → bus.publish_inbound(InboundMessage(...))                                  │
└──────────────────────────────────────────────────────────────────────────────┘


┌──────────────────────────── Agent 侧（agent/loop.py） ────────────────────────┐
│ agent.run() 常驻循环：                                                       │
│  msg = await bus.consume_inbound()                                           │
│  ├─ /stop /status 等控制命令直接处理                                          │
│  └─ 普通消息：create_task(_dispatch(msg))                                     │
│                                                                              │
│ _dispatch/_process_message/_run_agent_loop：                                   │
│  - 调 LLM，执行 tool_calls，publish_outbound(progress/stream/final)           │
│  - 保存 session + 触发 memory consolidation                                   │
└──────────────────────────────────────────────────────────────────────────────┘


┌──────────────────────────── Cron 侧（cron/service.py） ───────────────────────┐
│ cron.start() 常驻 ticking：                                                   │
│  到点 → 调用 cron.on_job(job)                                                │
│        → on_cron_job(...)                                                    │
│           ├─ resp = await agent.process_direct(reminder_note, session_key="cron:...")│
│           └─ （可选）evaluate_response → bus.publish_outbound(...)             │
└──────────────────────────────────────────────────────────────────────────────┘


┌──────────────────────────── Heartbeat 侧（heartbeat/service.py） ─────────────┐
│ heartbeat.start() 常驻 ticking：                                              │
│  到点 → on_execute(tasks)  → on_heartbeat_execute(...)                        │
│        → await agent.process_direct(tasks, session_key="heartbeat", ...)      │
│  完成 → on_notify(response) → on_heartbeat_notify(...) → bus.publish_outbound │
└──────────────────────────────────────────────────────────────────────────────┘
```

对应代码关键点（建议按关键字搜）：
- `nanobot/cli/commands.py`：`def gateway(...)`、`cron.on_job = on_cron_job`、`HeartbeatService(...)`、`asyncio.gather(agent.run(), channels.start_all())`
- `nanobot/channels/manager.py`：`start_all()` 与 `_dispatch_outbound()`
- `nanobot/channels/base.py`：`_handle_message()`（publish_inbound）
- `nanobot/agent/loop.py`：`run()`（consume_inbound）、`_dispatch()`（publish_outbound）
